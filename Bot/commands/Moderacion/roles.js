module.exports = {
    name: "kick",
    aliases: [],
    usage: "d!kick",
    description: "kickea a un usuario",
    run: async (client, message, args) => {
        
        if (message.content.startsWith("d!kick")) {
            
        }
        
    }
}